import { HasRightDirective } from './has-right.directive';

describe('HasRightDirective', () => {
  it('should create an instance', () => {
    const directive = new HasRightDirective();
    expect(directive).toBeTruthy();
  });
});
